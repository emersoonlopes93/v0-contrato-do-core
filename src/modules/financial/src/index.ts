import { manifest } from './manifest';
import { register } from './module';

export default {
  manifest,
  register,
};

export { manifest, register };
export { FINANCIAL_PERMISSIONS } from './permissions';
export type { FinancialPermission } from './permissions';

