import { manifest } from './manifest';
import { register } from './module';

const designerMenuModule = {
  manifest,
  register,
};

export default designerMenuModule;
export { manifest, register };

