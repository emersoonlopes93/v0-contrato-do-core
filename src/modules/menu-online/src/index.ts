import { manifest } from './manifest';
import { register } from './module';

export default {
  manifest,
  register,
};

export { manifest, register };
export { MENU_ONLINE_PERMISSIONS } from './permissions';
export type { MenuOnlinePermission } from './permissions';
