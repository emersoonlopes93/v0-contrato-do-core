import { manifest } from './manifest';
import { register } from './module';

export default {
  manifest,
  register,
};

export { manifest, register };
export { CUSTOMERS_CRM_PERMISSIONS } from './manifest';
export type { CustomersCrmPermission } from './manifest';
