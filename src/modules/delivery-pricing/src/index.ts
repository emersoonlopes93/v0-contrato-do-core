import { manifest } from './manifest';
import { register } from './module';

export default {
  manifest,
  register,
};

export { manifest, register };
export { DELIVERY_PRICING_PERMISSIONS } from './permissions';
export type { DeliveryPricingPermission } from './permissions';
